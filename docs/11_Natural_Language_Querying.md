# Natural Language Querying

Natural-language querying should translate user questions into governed SPARQL with schema awareness, permission checks, and query explanation. Generated SPARQL should be inspectable before execution in sensitive environments.

The query agent should use ontology metadata, known graph patterns, and validation constraints to reduce hallucinated predicates and unsafe query shapes.

