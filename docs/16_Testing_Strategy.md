# Testing Strategy

Testing should cover unit, integration, contract, and workflow behavior.

Initial targets:

- FastAPI endpoint tests.
- LangGraph workflow tests.
- RDF parsing and serialization tests.
- SHACL conformance tests.
- Graph store adapter integration tests.
- Frontend component and build checks.

Ontology and SHACL changes should include representative fixture graphs.

