services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    env_file:
      - .env.example
    environment:
      DATABASE_URL: postgresql+psycopg://semanticops:semanticops@postgres:5432/semanticops
      FUSEKI_URL: http://fuseki:3030/semanticops
      KNOWLEDGE_ASSETS_DIR: /app/kg
    ports:
      - "8000:8000"
    volumes:
      - ./kg:/app/kg:ro
    depends_on:
      postgres:
        condition: service_healthy
      fuseki:
        condition: service_started

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: semanticops
      POSTGRES_USER: semanticops
      POSTGRES_PASSWORD: semanticops
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U semanticops -d semanticops"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  fuseki:
    image: stain/jena-fuseki:latest
    environment:
      ADMIN_PASSWORD: semanticops
      FUSEKI_DATASET_1: semanticops
    ports:
      - "3030:3030"
    volumes:
      - fuseki-data:/fuseki

volumes:
  postgres-data:
  fuseki-data:
